default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: Niranjan@psql2026
  host: localhost

development:
  <<: *default
  database: my_first_rails_app_development

test:
  <<: *default
  database: my_first_rails_app_test

production:
  <<: *default
  database: my_first_rails_app_production
